/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'images.kabum.com.br', 'cdn.awsli.com.br', 'jcsbrasil.vteximg.com.br'],
    },
};

export default nextConfig;
